import { TouchNGoDirective } from './touch-n-go.directive';

describe('TouchNGoDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchNGoDirective();
    expect(directive).toBeTruthy();
  });
});
